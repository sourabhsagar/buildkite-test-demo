{
  "name": "buildkite-test-demo",
  "version": "1.0.0",
  "description": "Demo project to test BuildKite Test Engine with passing, failing, and flaky tests",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "buildkite-test-collector": "^1.0.0"
  }
}
